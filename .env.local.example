# ODPT (公共交通オープンデータセンター) のアクセストークン。
# .env.local にコピーして値を入れる。.env.local は .gitignore 済み。
#
# 使うのは npm run data:linemap だけ。ビルド時に運転系統の対応表を作るためで、
# 生成物 (data/line-map.json) にトークンは入らない。Pages にも載らない。
# 実行時に運行情報を取りに行く部分は別問題 (→ vault/宿題.md の「ODPT の鍵」)。
ODPT_TOKEN=
