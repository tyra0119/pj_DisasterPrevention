# 震度 → 路線への割り当て — 検証メモ

訪日外国人向けの単体 Web アプリ。地震発生から 30 分〜数時間の
「帰れるのか／飛べるのか」に答える。震度の数値ではなく、待つべきか動くべきかを出す。
GitHub Pages でリポジトリ直下をそのまま配信する（遅延レーダーと同じ）ので、
サーバもビルドも持たない。`src/` はプレーン JS で、型は JSDoc で持つ。

震度分布 → 路線への割り当て、までを実データで通した。
残るのは停止推定と ODPT 運行情報での確定。

## 結論

通る。全国 596 路線・27,343 地点への割り当てが **162ms**。
観測点名の座標解決は 199 イベント / 6,016 点で欠落ゼロ。

```
events: 199  points: 6016  resolved: 6016  unresolved: 0
rail: 596 lines / 10154 stations / 27343 sample points (N02-25)
event: 茨城県南部 M5.9 最大震度5-  観測点 1299 点
assigned 596 lines in 162ms
揺れた路線: 278 { '1': 77, '2': 25, '3': 35, '4': 55, '5-': 86 }
揺れなかった路線 (震度1未満): 318   判定不能: 0
```

`npm run verify:shindo` / `npm run verify:rail` で再現できる。

## データ源

| 用途 | 取得先 | 鍵 | CORS | ライセンス |
|---|---|---|---|---|
| 地震情報 | P2P地震情報 API v2 `api.p2pquake.net/v2/history` | 不要 | `*` | — |
| 同 (気象庁一次) | `www.jma.go.jp/bosai/quake/data/list.json` | 不要 | `*` | — |
| 観測点座標 | 気象庁「震度観測点一覧表」由来の公開ミラー | 不要 | — | — |
| 駅・路線 | 国土数値情報 鉄道データ N02-25 | 不要 | — | CC BY 4.0 |

気象庁 bosai の JSON は、気象庁自身が自サイト用に出しているもので、
XML と同じ内容が JSON 化済み・鍵なし・CORS 開放・`max-age=60`。
`en_ttl` / `en_anm` (英語の震源名) まで入っている。
「将来的に気象庁 XML へ」は XML パーサを書かずに今すぐ到達できる。

N02 は CC BY 4.0 なので、生成物を Pages に同梱して配れる。出典表示は要る。

## 同梱データ

`npm run data:build` で生成する。GitHub Pages は text を gzip 配信する。

| ファイル | 中身 | 生 | gzip |
|---|---|---|---|
| `data/shindo-stations.json` | 観測点 4,360 点 | 775KB | 86KB |
| `data/shindo-areas.json` | 細分区域 188 | 27KB | 6.6KB |
| `data/rail-lines.json` | 596 路線 / 10,154 駅 / 17,189 線形点 | 1.05MB | 289KB |

合計 gzip 382KB。Service Worker で丸ごとプリキャッシュできる範囲。

## 設計判断

**補間しない。** 震度は表層地盤で数百 m 単位に変わる。観測点の間を滑らかに埋めた値は
精度があるように見えて根拠がない。`sampleAt()` が返すのは
「半径内で実際に観測された最大震度」と、その観測点までの距離だけ。

**報告がないことは欠測ではない。** 気象庁の各地の震度は震度1以上を全点掲載するので、
半径内に観測点があるのに 1 点も報告がなければ、それは「震度1未満」という情報。
`confidence: 'below-threshold'` として `'unknown'` と区別する。
上の実行結果で「判定不能 0」なのはこの区別が効いているから。
ただしこれが言えるのは観測点単位の詳細情報が出てからなので、速報段階では `'unknown'` に倒す。

**路線は最大値を取る。** 事業者は区間ごと止めるので、平均ではなく線内最大。

**判定地点は駅 + 線形。** 駅だけだと駅間が数十 km ある路線 (北海道・山陰) で穴が空くので、
N02 の区間頂点を約 2km 格子に間引いて足す。判定半径が 20km なので、
これ以上細かく持っても結論は変わらない。

**格子索引。** 総当たりだと 27,343 地点 × 4,360 観測点で 1.2 億回。
0.25 度セルの格子で候補を絞って 162ms に収めた。

## 実行例 (2026-08-23 茨城県南部 M5.9 最大震度5弱)

```
震度5- 東京地下鉄 2号線日比谷線   最大: 北千住  (東京足立区伊興 5.0km)
震度5- 東武鉄道 伊勢崎線         最大: 南羽生  (茨城古河市下大野 18.5km)

京成電鉄 成田空港線              震度5- reported        最大: 千葉ニュータウン中央
東京モノレール 東京モノレール羽田空港線  震度5- reported        最大: モノレール浜松町
南海電気鉄道 空港線              震度-  below-threshold
西日本旅客鉄道 関西空港線          震度-  below-threshold
```

## 想定と違った点

**N02 の路線名は「線路名称」であって「運転系統」ではない。**
N02 の山手線は品川〜新宿〜田端の 17 駅しかない。東京〜品川は東海道線、
田端〜東京は東北線として別レコードに入っている。乗客が思う一周ではないし、
ODPT の運行情報は運転系統単位で来る。**運転系統 1 本 ↔ N02 路線 複数 の対応表が要る。**
震度の割り当て自体は最大値を取るだけなので、対応表さえあれば
運転系統側の値は N02 路線側の最大として合成できる。ここが次の実装の山。

**N02 に事業者名と路線名が入れ替わった行がある。**
`えちぜん鉄道 / 三国芦原線` が逆で入っている区間があり、駅ゼロの幽霊路線ができる。
駅を持たない路線を落として対処した。

**空港アクセス路線は N02 に揃っている。** 京成成田空港線・JR成田線・東京モノレール・
京急空港線・南海空港線・名鉄空港線・JR関西空港線・仙台空港鉄道。差別化点の土台は取れる。

## 残っている宿題

- **運転系統 ↔ N02 路線の対応表。** 上記のとおり次の山。ODPT と繋ぐ前に要る。
- **半径は問いによって変える。** 路線は 20km でよいが、「今いる場所」を同じ半径で答えると
  12km 先の観測点の値を返してしまい悲観的すぎる。滞在地は 5km 前後で分ける。
- **ODPT の鍵。** Pages は静的配信なので鍵が露出する。運行情報だけ薄いプロキシ
  (Cloudflare Workers など) に逃がすか、鍵込みで配るかの判断が要る。
  駅・路線のジオメトリは N02 で足りているので、ODPT は運行情報と航空便に絞れる。
- **観測点マスタはミラー経由。** 止まったら気象庁 CSV から作り直す
  (https://www.data.jma.go.jp/eqev/data/kyoshin/jma-shindo.html)。
- **停止推定のしきい値。** 各社の運転規制値 (一般に震度4〜5弱で規制、点検後再開) は
  公開情報にばらつきがある。ここはまだ手を付けていない。

## 構成

ビルドしないので、リポジトリ直下がそのままサイトのルートになる。

```
index.html                     疎通確認ページ (製品の画面ではない)
.nojekyll                      Jekyll を通さない
data/*.json                    同梱データ (生成物だがコミットする)
src/                           ブラウザが直接読む ES モジュール
  data-url.js                  同梱データの URL 解決 (<base href="./"> 基準)
  quake/types.js               震度階級・イベントの型 (JSDoc)
  quake/stations.js            観測点マスタの索引
  quake/grid.js                緯度経度の格子索引
  quake/p2p.js                 P2P v2 → QuakeEvent 正規化
  quake/field.js               座標・区間への当てはめ
  rail/types.js                路線マスタの型 (JSDoc)
  rail/lines.js                路線マスタの読み込み
  rail/assign.js               震度 → 路線への割り当て

scripts/                       ビルド時のみ。配信には不要 (置いてあるだけ)
  build-shindo-data.mjs        観測点マスタ生成    npm run data:shindo
  build-rail-data.mjs          路線マスタ生成      npm run data:rail
  lib/unzip.mjs                N02 の ZIP 展開 (依存ゼロ)
  verify-spatialize.mjs        震度の空間化を検証  npm run verify:shindo
  verify-rail.mjs              路線割り当てを検証  npm run verify:rail

jsconfig.json                  JSDoc の型検査だけの設定  npx tsc
.cache/                        N02 の zip 15MB (gitignore 済み)
```

**`<base href="./">`** を `index.html` に入れてある。Pages のプロジェクトページは
`/<repo>/` 配下に載るので絶対パスだと外れるが、相対基準にしておけば
ローカルの `python -m http.server` でも同じ URL 解決になる。
パスで画面を分けるようになったら絶対パスに変えること
(いまは URL クエリだけで済ませる前提なので `./` で足りる)。

**`npm i` は要らない。** 依存ゼロで、Node もデータ生成と検証のときだけ使う。

---

出典: 国土数値情報（鉄道データ）国土交通省 — CC BY 4.0 /
気象庁 震度観測点一覧表 / P2P地震情報 API v2
