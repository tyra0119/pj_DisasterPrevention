# スクリーンショットと動画の準備

フォームに添付する素材。撮ったものはこのフォルダの `素材/` に入れる（個人情報が写らないこと）。

## できているもの (2026-09-06)

| ファイル | 中身 | 作り方 |
|---|---|---|
| `素材/01〜09_*.png` | 下表のスクリーンショット 10 枚 (390×844 @2x) | `生成スクリプト/shoot.mjs`。**赤いテスト帯は資料用に隠してある**（本物の画面では出る。説明文に一言添える） |
| `紹介スライド.pptx` | **動画版 11 枚**。ノートに 120 秒用の原稿 | `生成スクリプト/build_deck.py`。文言を直すならこれを直す |
| `紹介スライド_詳細版.pptx` | 14 枚（内訳・事前設定・技術を含む）。最終プレゼン用 | 同じスクリプトから。原稿は長い版 |
| `紹介スライド_ナレーション付き.pptx` | 上に音声 (Windows 音声合成・Haruka) を埋め、切替時間を付けたもの | `生成スクリプト/make_video.ps1` の途中生成物。git 管理外 (7.7MB) |
| `素材/サムネイル.png` | 動画のサムネイル 1280×720。**止まった電車の前で不安そうな旅行者のイラスト** | `生成スクリプト/thumb_illust.html`（SVG を Chrome で書き出し） |
| `素材/サムネイル_画面版.png` | 差し替え前のサムネイル。判定帯の赤にスマホ画面 | `生成スクリプト/thumb.py` |
| `紹介動画.mp4` | **1 分 59 秒**、1080p 30fps | `生成スクリプト/make_video.ps1`。git 管理外 (28MB) |

**作り直しは `生成スクリプト/` から。**手で作り直さない。→ `生成スクリプト/README.md`

## 公開先

動画は YouTube に**限定公開**で上げてある (2026-09-10)。フォームにはこの URL を貼る。
限定公開はリンクを知っていれば誰でも見られる。審査員に渡すぶんには足りる。

https://www.youtube.com/watch?v=QI5p62Jye2U

**作り直したら上げ直して、URL が変わっていないか確かめる。**サムネイルは YouTube Studio で差し替える（URL は変わらない）。上の表のファイルと
この URL の中身がずれると、審査員が見るのは古い方になる。

**この URL は公開リポジトリに載っている。**限定公開でも、リポジトリを見れば辿れる。
中身は公開アプリの紹介なので困らないと判断したが、本当に伏せたいなら
別の動画として上げ直し、URL はここに書かずフォームへ直接入力する
（一度 push した URL は履歴に残り、force push をしない方針では消せない）。

合成音声のままでは硬いので、提出前に自分の声を録り直すか、少なくとも聞いて違和感のある箇所の原稿を直す。
原稿は `生成スクリプト/build_deck.py` の `notes(...)`（長い版）と `SHORT`（120 秒版）。

## スクリーンショット（スマホ幅 390×844 目安）

| # | 場面 | URL (`?` 以降) | 写すもの |
|---|---|---|---|
| 1 | 判定帯（5 弱・英語） | `lang=en&scenario=tokyo-5minus&home=35.6885,139.6990&homeName=Shinjuku&flight=NRT` | 帯・再開までの幅と時刻・「get back tonight」・3 行の場所 |
| 2 | 路線の内訳を開いた状態 | 同上で「Your accommodation」をタップ、1 路線の「+」も開く | 路線ごとの印、震度・点検区間・再開までの根拠 |
| 3 | 6 強・宿を取れ | `lang=en&scenario=tokyo-6plus&home=35.6812,139.7671&homeName=Tokyo&flight=NRT` | 「RAIL LIKELY UNUSABLE」「book a room early」 |
| 4 | 休める場所の地図 | 同上で「Where you can rest if stranded」を開く | 一時滞在施設（青）と指定緊急避難場所（緑）が同じ地図 |
| 5 | 震源の地図 | 同上で「地図で見る」 | 震源・観測点 |
| 6 | 設定ダイアログ | 右上「設定」 | 3 つの枠、駅名検索、言語、設定つきリンク |
| 7 | 日本語 | #1 を `lang=ja` | 4 言語のうち 1 枚 |
| 8 | 中文 / 한국어 | #1 を `lang=zh` / `lang=ko` | 同上（余裕があれば） |
| 9 | 実際の地震 | `lang=ja&event=6a89d870e88ee598246bf24d&home=35.6812,139.7671&homeName=%E6%9D%B1%E4%BA%AC&flight=HND` | 実データでの推定。テスト帯が無いこと |

撮るときの注意:
- 実データ画面 (#9) 以外は赤い「テスト表示」帯が写る。これは意図どおり（実地震と混同させない）。説明文に一言添える
- 位置情報は許可して、住所が出た状態で撮る
- 時刻が写るので、再開の時刻帯は撮った時刻と矛盾しないように（テストデータは「発生 40 分前」で作られる）

## 紹介動画（1〜3 分）

構成は `02_作品紹介.md` の順。ナレーション原稿もそこから取る。

| 秒 | 画面 | 言うこと |
|---|---|---|
| 0–15 | 鉄道会社の案内文（テキスト） | 「運転再開の見込みは立っていません」。2026-08-23、4 時間以上この状態だった |
| 15–30 | 訪日外国人の状況（テキストか写真） | 読めない・「点検」の長さを知らない・ホテルが埋まるのを知らない |
| 30–60 | #1 の画面 | 帯 → 幅と時刻 → 「今夜戻れる」。宿をタップして内訳。路線の「+」で根拠 |
| 60–80 | #3 → #4 | 6 強なら「宿を取れ」。休める場所の地図 |
| 80–100 | 仕組みの図（`02_作品紹介.md` §4） | 震度 → 路線 → ODPT で運転系統 → 推定式 → ODPT 運行情報で終わらせる |
| 100–120 | #9 実地震 | 実データでの幅。実際も 4 時間超えていた |
| 120–140 | 設定と共有リンク | 宿・便を事前に。リンクを配れる。4 言語 |
| 140–160 | まとめ | 「あとどれくらい」を出しているのはここだけ。待つか、寝場所を確保するか、いま決められる |

録画: ブラウザをスマホ幅にして画面録画。音声は後で載せる。字幕は英語も付けると審査員に伝わりやすい。

## 図

- 仕組みの図は `02_作品紹介.md` §4 のテキスト図をそのまま Mermaid か手描きに起こす
- 2026-08-23 の推定表（山手線 1 時間 44 分〜5 時間 35 分 など）は `NOTES.md` の表から
